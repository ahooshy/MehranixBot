services:
  - type: worker
    name: jarvis-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: PPLX_API_KEY
        sync: false
